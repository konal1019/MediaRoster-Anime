{
  "name": "anime-list",
  "version": "1.0.0",
  "description": "A simple HTML/JS/CSS starter template",
  "main": "fetcher.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hash": "python hash.py",
    "build": "npm run hash",
    "deploy": "npm run build && git add -f build && git commit -m 'temp build commit' && git subtree push --prefix build origin gh-pages --force && git reset --hard HEAD~ && git clean -fd && rm -rf build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-fetch": "^3.3.2"
  }
}
