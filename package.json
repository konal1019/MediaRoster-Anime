{
  "name": "anime-list",
  "version": "1.0.0",
  "description": "A simple HTML/JS/CSS starter template",
  "main": "fetcher.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hash": "python hash.py",
    "build": "npm run hash",
    "deploy": "npm run build && git push origin `git subtree split --prefix build main`:refs/heads/gh-pages --force && rm -rf build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-fetch": "^3.3.2"
  }
}
